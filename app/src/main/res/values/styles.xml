<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="textFR">
        <item name="android:textColor">@color/colorStartScreenText</item>
        <item name="android:textSize">36sp</item>
    </style>

    <style name="buttonGoogle">
        <item name="android:text">@string/buttonGoogle</item>
    </style>

    <style name="buttonSignOut">
        <item name="android:backgroundTint">@color/colorButtonGoogle</item>
    </style>

    <style name="toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="checkBox">
        <item name="colorControlNormal">?attr/colorPrimary</item>
        <item name="colorControlActivated">?attr/colorPrimary</item>
    </style>

    <style name="textEmailAddress">
    <item name="colorControlNormal">@color/colorTextHintNormal</item>
    <item name="colorControlActivated">@color/colorTextHintActivated</item>
    </style>

    <style name="buttonSendInquiry">
        <item name="android:text">@string/buttonSignOutAccount</item>
        <item name="android:textColor">@color/colorStartScreenText</item>
        <item name="colorButtonNormal">@color/colorButtonAddContact</item>
    </style>

    <style name="textProfile">
        <item name="android:text">@string/textNameProfile</item>
        <item name="android:textColor">@color/colorTextProfile</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="emailProfile">
        <item name="android:text">@string/emailProfile</item>
        <item name="android:textColor">@color/colorEmailProfile</item>
        <item name="android:textSize">12dp</item>
    </style>

</resources>
